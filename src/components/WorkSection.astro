---
const projects = [
  {
    title: "Mindfulness App",
    description: "A meditation companion app designed to bring peace and clarity to daily life.",
    tags: ["React", "Node.js", "MongoDB"],
    link: "#"
  },
  {
    title: "Writing Platform",
    description: "A minimalist platform for sharing thoughts and essays on technology and philosophy.",
    tags: ["Astro", "TypeScript", "MDX"],
    link: "#"
  },
  {
    title: "Open Source Tools",
    description: "Contributing to tools that make developers' lives easier and more productive.",
    tags: ["Python", "JavaScript", "CLI"],
    link: "#"
  }
];
---

<section id="work" class="fade-in">
  <h2 class="section-title">Featured Work</h2>
  <p class="section-subtitle">Projects and creations I'm passionate about</p>
  <div class="work-grid">
    {projects.map(project => (
      <div class="work-card">
        <h3>{project.title}</h3>
        <p>{project.description}</p>
        <div class="tags">
          {project.tags.map(tag => (
            <span class="tag">{tag}</span>
          ))}
        </div>
      </div>
    ))}
  </div>
</section>

<style>
  section {
    padding: 5rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    text-align: center;
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-subtitle {
    text-align: center;
    color: var(--text-secondary);
    margin-bottom: 3rem;
  }

  .work-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .work-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 15px;
    padding: 2rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    cursor: pointer;
  }

  .work-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--gradient);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }

  .work-card:hover::before {
    transform: translateX(0);
  }

  .work-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    border-color: var(--accent);
  }

  .work-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .work-card p {
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
  }

  .tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .tag {
    padding: 0.25rem 0.75rem;
    background: rgba(99, 102, 241, 0.1);
    border: 1px solid rgba(99, 102, 241, 0.3);
    border-radius: 20px;
    font-size: 0.875rem;
    color: var(--accent-hover);
  }
</style>