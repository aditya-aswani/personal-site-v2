---
const projects = [
  {
    title: "Contemplative Semester",
    description: "A three-month immersion in meditation, community living and earth connection for young adults.",
    link: "https://www.contemplativesemester.org/"
  },
  {
    title: "PausePal",
    description: "Find your meditation accountability buddy.",
    tags: ["React", "Node.js", "MongoDB"],
    link: "https://pausepal.co/"
  },
  {
    title: "Brooklyn Insight Sangha",
    description: "Website for my local meditation community.",
    link: "https://bkinsight.nyc/"
  },
  {
    title: "One-on-One Meditation Guidance",
    description: "Get personalized meditation guidance on a donation basis.",
    link: "/learn-meditation"
  }
];
---

<section id="work" class="fade-in">
  <h2 class="section-title">What I'm Working On</h2>
  <div class="work-grid">
    {projects.map(project => (
      <a href={project.link} class="work-card" target="_blank" rel="noopener noreferrer">
        <h3>{project.title}</h3>
        <p>{project.description}</p>
        <div class="card-arrow">→</div>
      </a>
    ))}
  </div>
</section>

<style>
  section {
    padding: 5rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    text-align: center;
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-subtitle {
    text-align: center;
    color: var(--text-secondary);
    margin-bottom: 3rem;
  }

  .work-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .work-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 15px;
    padding: 2rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .work-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--gradient);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }

  .work-card:hover::before {
    transform: translateX(0);
  }

  .work-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    border-color: var(--accent);
  }

  .work-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--text-primary);
    line-height: 1.3;
    min-height: 2.6rem; /* Ensures consistent height for both titles */
    display: flex;
    align-items: flex-start;
  }

  .work-card p {
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  .card-arrow {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    color: var(--text-secondary);
    font-size: 1.5rem;
    transition: all 0.3s ease;
    opacity: 0.5;
  }

  .work-card:hover .card-arrow {
    opacity: 1;
    transform: translate(3px, -3px);
  }
</style>